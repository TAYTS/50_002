module checker (
    input expect[2],  // expected value
    input raw[2],     // actual value
    output pass[1]
  ) {

  always {
    if (raw == expect)
      pass = 1b1;
    else
      pass = 1b0;
  }
}
